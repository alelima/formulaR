
class Java::JavaMath::BigDecimal
  
  def + (operand)
    self.add(operand);
  end
  
  def - (operand)
    self.subtract(operand);
  end
  
  def * (operand)
    self.multiply(operand);      
  end
      
  def / (operand)
    self.divide(operand);      
  end
    
end
